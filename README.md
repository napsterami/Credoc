# fuzzyIF

1, To load the individual dataset use the file Data_to_upload.py
2. Then Run fast_FIF_main.py. Threshold parameter is "TH" chosen as 0.2
3. In the file fast_FIF.py parameter "ratio" is beta i.e the percentage of margin.   

For Isolation Forest,

Just Run if_FUNCTION.py
